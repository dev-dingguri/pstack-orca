---
name: run-role
description: dispatch one role to its configured entries, native or Orca worker
disable-model-invocation: true
---

Invoke the `run-role` skill and follow it. Resolve Claude tool names, `claude-*` model slugs, and Claude built-in skills through `poteto-mode/references/codex-tools.md`, including its Per-skill notes.
